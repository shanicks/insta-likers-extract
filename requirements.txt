# Runtime dependency. requests pulls in certifi, charset-normalizer, idna, urllib3.
requests==2.32.5

# boto3 is used (S3 / Secrets Manager / SES) but is pre-installed in the AWS
# Lambda Python runtime, so it does NOT need to be bundled or layered.
# Install it only for local development:
#   pip install boto3
